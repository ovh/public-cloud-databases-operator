---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: service.clouddatabases.ovhcloud.net
spec:
  group: service.clouddatabases.ovhcloud.net
  names:
    kind: service
    listKind: serviceList
    plural: services
    singular: service
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: service is the Schema for the services
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: serviceSpec defines the desired state of service
            properties:
              labelSelector:
                type: object
                description: A label selector is a label query over a set of resources. The result of matchLabels and matchExpressions are ANDed. An empty label selector matches all objects. A null label selector matches no objects
                properties:
                  matchLabels:
                    type: object
                    description: matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is \"key\", the operator is \"In\", and the values array contains only \"value\". The requirements are ANDed
              projectId:
                description: ProjectId is the Id of the project on which ips are to
                  be autorized
                type: string
              serviceId:
                description: ProjectId is the Id of the specific service on which
                  ips are to be autorized
                type: string
            type: object
          status:
            description: serviceStatus defines the observed state of
              service
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
